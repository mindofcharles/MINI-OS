# Host Applications

This directory contains interactive applications that run on the development host and communicate with MINI-OS.

These programs are not build, image, or system-inspection tools and therefore do not belong in `tools/`.

Each application owns its implementation and focused software tests in one component directory.
